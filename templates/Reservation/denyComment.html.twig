{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(form) }}
    <button type="submit" onclick="location.href = '{{ path('pendingJobs') }}'">Siųsti</button>
    {{ form_end(form) }}


{% endblock %}