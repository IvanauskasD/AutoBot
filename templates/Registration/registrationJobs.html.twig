{% extends 'base.html.twig' %}

{% block body %}
    <body>
    <div class="container">
        <div class="row">
            <div class="col"></div>
            <div class="col-5">
                <h1>Darbu registracija</h1>
                <div style="color: red">
                    {#{% for message in error %}#}
                    {#{{ message }}<br>#}
                    {#{% endfor %}#}
                </div>
                <div id="default">
                    {{ form_start(jobs_form) }}
                    {{ form_row(jobs_form.jobsName) }}
                    <button class="btn btn-primary">Išsaugoti</button>
                    {{ form_end(jobs_form) }}
                </div>
            </div>
            <div class="col"></div>

        </div>
    </div>


    </body>


{%  endblock %}
